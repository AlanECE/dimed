pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,jsx,ts,tsx,json}"
      run: pnpm biome check --write {staged_files}
      stage_fixed: true
    ruff-check:
      glob: "*.py"
      run: cd apps/api && uv run ruff check {staged_files}
    ruff-format:
      glob: "*.py"
      run: cd apps/api && uv run ruff format --check {staged_files}
