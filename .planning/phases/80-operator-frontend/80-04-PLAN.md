---
phase: 80
plan: 4
title: Camions CRUD page + Route sheets page
wave: 2
depends_on: [1, 2]
files_modified:
  - apps/web/app/(authenticated)/dashboard/camions/page.tsx
  - apps/web/app/(authenticated)/dashboard/routes/page.tsx
  - apps/web/components/camion-dialog.tsx
  - apps/web/hooks/use-camions.ts
  - apps/web/hooks/use-route-sheets.ts
  - apps/web/lib/types.ts
autonomous: true
requirements:
  - OPER-03
---

# Plan 04: Camions CRUD page + Route sheets page

## Goal
Build the truck management page (CRUD) and the route sheets visualization page.

## Tasks

<task id="04.1">
### Add new types and camions hook

<read_first>
- apps/web/lib/types.ts (existing types)
- apps/web/lib/api.ts (fetchApi)
- .planning/phases/80-operator-frontend/80-RESEARCH.md (API Contracts)
</read_first>

<action>
1. Add to `apps/web/lib/types.ts`:
   ```typescript
   export type CamionResponse = {
     id: string
     nom: string
     plaque: string
     created_at: string
   }

   export type FeuilleDeRouteResponse = {
     id: string
     camion_id: string
     camion_nom: string
     date: string
     ligne: string | null
     compteurs: {
       colis_std: number
       sachets_std: number
       colis_frg: number
       sachets_frg: number
     }
     commandes: OrderResponse[]
   }
   ```

2. Create `apps/web/hooks/use-camions.ts`:
   - `useCamions()`: fetch GET /camions, return {camions, loading, error, refetch}
   - `createCamion(nom, plaque)`: POST /camions
   - `updateCamion(id, data)`: PATCH /camions/{id}
   - `deleteCamion(id)`: DELETE /camions/{id}
</action>

<acceptance_criteria>
- apps/web/lib/types.ts exports CamionResponse, FeuilleDeRouteResponse
- apps/web/hooks/use-camions.ts exports useCamions with CRUD operations
- `pnpm --filter @dimed/web typecheck` exits 0
</acceptance_criteria>
</task>

<task id="04.2">
### Build camion dialog and CRUD page

<read_first>
- apps/web/hooks/use-camions.ts
- apps/web/components/ui/dialog.tsx
- apps/web/components/ui/alert-dialog.tsx
- apps/web/components/ui/table.tsx
- .planning/phases/80-operator-frontend/80-UI-SPEC.md (Camions CRUD layout)
</read_first>

<action>
1. Create `apps/web/components/camion-dialog.tsx`:
   - Props: `{ open, onOpenChange, camion?: CamionResponse, onSubmit }`
   - Dialog with title "Nouveau camion" or "Modifier le camion"
   - Fields: nom (Input, label "Nom / identifiant"), plaque (Input, label "Plaque d'immatriculation")
   - Submit button: "Ajouter" or "Enregistrer"
   - Loading state on submit

2. Update `apps/web/app/(authenticated)/dashboard/camions/page.tsx`:
   - Title: h2 "Camions"
   - Button "Ajouter un camion" (top right, primary)
   - Table: columns Nom, Plaque, Actions (edit Pencil + delete Trash2 ghost buttons)
   - Edit: opens CamionDialog with pre-filled data
   - Delete: AlertDialog "Supprimer ce camion ? Cette action est irréversible."
   - Empty state: "Aucun camion. Ajoutez un camion pour commencer."
   - Toast feedback for create/update/delete
   - Skeleton loading
</action>

<acceptance_criteria>
- apps/web/components/camion-dialog.tsx renders Dialog with nom + plaque inputs
- Camions page has table with Nom, Plaque, Actions columns
- "Ajouter un camion" button opens dialog
- Edit button opens dialog with camion data
- Delete button opens AlertDialog confirmation
- Empty state shows "Aucun camion. Ajoutez un camion pour commencer."
- Toast on create/update/delete success
- `pnpm --filter @dimed/web typecheck` exits 0
</acceptance_criteria>
</task>

<task id="04.3">
### Build route sheets page

<read_first>
- apps/web/lib/api.ts (fetchApi)
- apps/web/lib/types.ts (FeuilleDeRouteResponse)
- apps/web/components/status-badge.tsx
- .planning/phases/80-operator-frontend/80-UI-SPEC.md (Feuilles de route layout)
</read_first>

<action>
1. Create `apps/web/hooks/use-route-sheets.ts`:
   - `useRouteSheets()`: fetch GET /documents/feuilles-route
   - Returns {sheets: FeuilleDeRouteResponse[], loading, error, refetch}

2. Update `apps/web/app/(authenticated)/dashboard/routes/page.tsx`:
   - Title: h2 "Feuilles de route"
   - Cards layout: one Card per camion/feuille
   - Each card shows:
     - Header: camion nom (plaque), feuille reference, nombre de commandes
     - Body: list of commandes (référence, pharmacien, montant) — compact rows
     - Footer: compteurs inline (colis_std, sachets_std, colis_frg, sachets_frg)
     - "Voir PDF" ghost button (link to /documents/feuilles-route/{id}/pdf — if available)
   - Camions without feuille: show "Pas de commandes assignées"
   - Empty state: "Aucun camion configuré. Ajoutez un camion d'abord."
   - Skeleton loading (3 card skeletons)
</action>

<acceptance_criteria>
- apps/web/hooks/use-route-sheets.ts exports useRouteSheets
- Route sheets page renders cards, one per camion
- Each card shows camion info, commandes list, compteurs
- "Voir PDF" button present
- Empty camion shows "Pas de commandes assignées"
- Global empty state shows "Aucun camion configuré"
- `pnpm --filter @dimed/web typecheck` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
pnpm --filter @dimed/web typecheck
pnpm --filter @dimed/web build
pnpm --filter @dimed/web lint
```

## must_haves
- Camion CRUD page with add/edit/delete via dialogs
- Route sheets page with cards per camion showing commandes and compteurs
- New types (CamionResponse, FeuilleDeRouteResponse) in shared types
