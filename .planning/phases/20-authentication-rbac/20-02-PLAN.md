---
phase: 20
plan: 02
title: "Auth & admin API endpoints"
wave: 2
depends_on: [01]
files_modified:
  - apps/api/app/auth/router.py
  - apps/api/app/admin/__init__.py
  - apps/api/app/admin/router.py
  - apps/api/app/main.py
  - apps/api/app/cli.py
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05
---

# Plan 02: Auth & Admin API Endpoints

## Goal
Create the auth router (login, refresh, logout, me) and admin router (CRUD users), wire them into the FastAPI app, and create the seed admin CLI script.

## Tasks

<task id="02.1">
<title>Create auth router (login, refresh, logout, me)</title>
<read_first>
- apps/api/app/auth/service.py (JWT functions, Redis operations)
- apps/api/app/auth/dependencies.py (get_current_user, CurrentUser)
- apps/api/app/auth/schemas.py (LoginRequest, UserResponse)
- apps/api/app/models/user.py (User model)
</read_first>
<action>
Create `apps/api/app/auth/router.py`:

Endpoints:
1. **POST /auth/login** — Accept email+password in JSON body. Verify against DB. If valid: create access_token + refresh_token, set as httpOnly cookies, return UserResponse. If invalid: 401.
2. **POST /auth/refresh** — Read refresh_token from cookie. Validate in Redis. If valid: rotate refresh token, create new access_token, set new cookies. If invalid: 401.
3. **POST /auth/logout** — Read access_token cookie, decode to get jti, blacklist in Redis. Delete refresh_token from Redis. Clear both cookies. Return 200.
4. **GET /auth/me** — Protected endpoint. Return current user as UserResponse.

Cookie settings:
- `httponly=True`
- `secure=False` for dev (set via config: `settings.cookie_secure`)
- `samesite="lax"`
- access_token path: `/`
- refresh_token path: `/auth/refresh`

Add `cookie_secure: bool = False` to Settings in config.py.
</action>
<acceptance_criteria>
- `apps/api/app/auth/router.py` contains `@router.post("/login")`
- `apps/api/app/auth/router.py` contains `@router.post("/refresh")`
- `apps/api/app/auth/router.py` contains `@router.post("/logout")`
- `apps/api/app/auth/router.py` contains `@router.get("/me")`
- `apps/api/app/auth/router.py` contains `response.set_cookie`
- `apps/api/app/auth/router.py` contains `httponly=True`
- `apps/api/app/config.py` contains `cookie_secure: bool`
</acceptance_criteria>
</task>

<task id="02.2">
<title>Create admin router (CRUD users)</title>
<read_first>
- apps/api/app/auth/dependencies.py (require_roles)
- apps/api/app/auth/schemas.py (AdminUserCreate, AdminUserUpdate, UserResponse)
- apps/api/app/auth/service.py (hash_password)
- apps/api/app/models/user.py (User, UserRole)
</read_first>
<action>
Create `apps/api/app/admin/__init__.py` (empty).

Create `apps/api/app/admin/router.py`:

Endpoints (all require admin role):
1. **POST /admin/users** — Create user. Hash password, create User in DB. Return UserResponse + 201. Reject if email already exists (409).
2. **GET /admin/users** — List all users, paginated (limit/offset query params). Return list of UserResponse.
3. **PATCH /admin/users/{user_id}** — Update user role, is_active, nom. Return updated UserResponse. 404 if not found.

All endpoints use `require_roles(["admin"])` dependency.
</action>
<acceptance_criteria>
- `apps/api/app/admin/router.py` contains `@router.post("/users")`
- `apps/api/app/admin/router.py` contains `@router.get("/users")`
- `apps/api/app/admin/router.py` contains `@router.patch("/users/{user_id}")`
- `apps/api/app/admin/router.py` contains `require_roles(["admin"])`
</acceptance_criteria>
</task>

<task id="02.3">
<title>Wire routers into FastAPI app</title>
<read_first>
- apps/api/app/main.py (current state)
- apps/api/app/auth/router.py (created in 02.1)
- apps/api/app/admin/router.py (created in 02.2)
</read_first>
<action>
Update `apps/api/app/main.py`:

```python
from app.auth.router import router as auth_router
from app.admin.router import router as admin_router

app.include_router(auth_router, prefix="/auth", tags=["auth"])
app.include_router(admin_router, prefix="/admin", tags=["admin"])
```
</action>
<acceptance_criteria>
- `apps/api/app/main.py` contains `include_router(auth_router`
- `apps/api/app/main.py` contains `include_router(admin_router`
- `apps/api/app/main.py` contains `prefix="/auth"`
- `apps/api/app/main.py` contains `prefix="/admin"`
</acceptance_criteria>
</task>

<task id="02.4">
<title>Create seed admin CLI script</title>
<read_first>
- apps/api/app/auth/service.py (hash_password)
- apps/api/app/models/user.py (User, UserRole)
- apps/api/app/db/session.py (engine, async_session)
</read_first>
<action>
Create `apps/api/app/cli.py`:

```python
"""CLI for DIMED API administration.

Usage:
    python -m app.cli create-admin --email admin@dimed.dz --password <pwd>
"""
import argparse
import asyncio
from uuid import uuid4

from sqlalchemy import select

from app.auth.service import hash_password
from app.db.session import async_session
from app.models.user import User, UserRole


async def create_admin(email: str, password: str) -> None:
    async with async_session() as session:
        result = await session.execute(select(User).where(User.email == email))
        if result.scalar_one_or_none():
            print(f"Admin {email} already exists, skipping.")
            return

        admin = User(
            id=uuid4(),
            email=email,
            password_hash=hash_password(password),
            role=UserRole.ADMIN,
            nom="Administrator",
            is_active=True,
        )
        session.add(admin)
        await session.commit()
        print(f"Admin {email} created successfully.")


def main() -> None:
    parser = argparse.ArgumentParser(description="DIMED API CLI")
    subparsers = parser.add_subparsers(dest="command")

    create_admin_parser = subparsers.add_parser("create-admin")
    create_admin_parser.add_argument("--email", required=True)
    create_admin_parser.add_argument("--password", required=True)

    args = parser.parse_args()

    if args.command == "create-admin":
        asyncio.run(create_admin(args.email, args.password))
    else:
        parser.print_help()


if __name__ == "__main__":
    main()
```

Also create `apps/api/app/__main__.py`:
```python
from app.cli import main
main()
```
</action>
<acceptance_criteria>
- `apps/api/app/cli.py` contains `async def create_admin`
- `apps/api/app/cli.py` contains `hash_password(password)`
- `apps/api/app/cli.py` contains `UserRole.ADMIN`
- `apps/api/app/__main__.py` contains `from app.cli import main`
</acceptance_criteria>
</task>

## Verification
```bash
cd apps/api && uv run ruff check . && uv run ruff format --check .
cd apps/api && uv run python -c "from app.auth.router import router; print(f'Auth router: {len(router.routes)} routes')"
cd apps/api && uv run python -c "from app.admin.router import router; print(f'Admin router: {len(router.routes)} routes')"
# With Docker running:
# curl -X POST http://localhost:8000/auth/login -d '{"email":"admin@dimed.dz","password":"test"}' -H 'Content-Type: application/json'
```

## must_haves
- Login endpoint sets httpOnly cookies
- Refresh endpoint rotates refresh token
- Logout blacklists tokens and clears cookies
- Admin endpoints require admin role (403 for others)
- CLI creates seed admin user
- All routers wired into main app
