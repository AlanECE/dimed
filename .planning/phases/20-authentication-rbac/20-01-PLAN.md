---
phase: 20
plan: 01
title: "Auth service, JWT tokens & Redis blacklist"
wave: 1
depends_on: []
files_modified:
  - apps/api/app/auth/__init__.py
  - apps/api/app/auth/service.py
  - apps/api/app/auth/schemas.py
  - apps/api/app/auth/dependencies.py
  - apps/api/app/models/user.py
  - packages/shared-types/src/index.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
---

# Plan 01: Auth Service, JWT Tokens & Redis Blacklist

## Goal
Create the core auth service layer: password hashing, JWT token creation/validation, Redis blacklist, and FastAPI dependencies for current user extraction and role checking.

## Tasks

<task id="01.1">
<title>Add admin role to UserRole enum</title>
<read_first>
- apps/api/app/models/user.py
- packages/shared-types/src/index.ts
</read_first>
<action>
In `apps/api/app/models/user.py`, add `ADMIN = "admin"` as first entry in `UserRole` StrEnum.

In `packages/shared-types/src/index.ts`, add `"admin"` to the `UserRole` union type.
</action>
<acceptance_criteria>
- `apps/api/app/models/user.py` contains `ADMIN = "admin"`
- `packages/shared-types/src/index.ts` contains `"admin"`
- `uv run ruff check .` passes in apps/api
</acceptance_criteria>
</task>

<task id="01.2">
<title>Create auth schemas (Pydantic models)</title>
<read_first>
- apps/api/app/models/user.py (UserRole enum)
</read_first>
<action>
Create `apps/api/app/auth/__init__.py` (empty).

Create `apps/api/app/auth/schemas.py`:
```python
from pydantic import BaseModel, EmailStr
from app.models.user import UserRole

class LoginRequest(BaseModel):
    email: str
    password: str

class TokenPayload(BaseModel):
    sub: str  # user_id as string
    role: str
    email: str
    jti: str  # JWT ID for blacklisting
    type: str  # "access" or "refresh"

class UserResponse(BaseModel):
    id: str
    email: str
    role: UserRole
    nom: str
    is_active: bool

    model_config = {"from_attributes": True}

class AdminUserCreate(BaseModel):
    email: str
    password: str
    role: UserRole
    nom: str
    adresse: str | None = None
    secteur: str | None = None

class AdminUserUpdate(BaseModel):
    role: UserRole | None = None
    is_active: bool | None = None
    nom: str | None = None
```
</action>
<acceptance_criteria>
- `apps/api/app/auth/schemas.py` contains `class LoginRequest(BaseModel)`
- `apps/api/app/auth/schemas.py` contains `class TokenPayload(BaseModel)`
- `apps/api/app/auth/schemas.py` contains `class UserResponse(BaseModel)`
- `apps/api/app/auth/schemas.py` contains `class AdminUserCreate(BaseModel)`
</acceptance_criteria>
</task>

<task id="01.3">
<title>Create auth service (JWT + password + Redis)</title>
<read_first>
- apps/api/app/config.py (settings: secret_key, expiry, redis_url)
- apps/api/app/auth/schemas.py (created in 01.2)
</read_first>
<action>
Create `apps/api/app/auth/service.py`:

```python
from datetime import datetime, timedelta, timezone
from uuid import uuid4

import redis
from jose import JWTError, jwt
from passlib.hash import bcrypt

from app.config import settings

ALGORITHM = "HS256"

redis_client = redis.Redis.from_url(settings.redis_url, decode_responses=True)


def hash_password(password: str) -> str:
    return bcrypt.hash(password)


def verify_password(password: str, password_hash: str) -> bool:
    return bcrypt.verify(password, password_hash)


def create_access_token(user_id: str, role: str, email: str) -> str:
    jti = str(uuid4())
    payload = {
        "sub": user_id,
        "role": role,
        "email": email,
        "jti": jti,
        "type": "access",
        "exp": datetime.now(timezone.utc) + timedelta(minutes=settings.access_token_expire_minutes),
    }
    return jwt.encode(payload, settings.secret_key, algorithm=ALGORITHM)


def create_refresh_token(user_id: str) -> str:
    token_id = str(uuid4())
    # Store in Redis: refresh:{token_id} -> user_id, TTL = refresh_token_expire_days
    ttl = settings.refresh_token_expire_days * 24 * 60 * 60
    redis_client.setex(f"refresh:{token_id}", ttl, user_id)
    return token_id


def validate_refresh_token(token_id: str) -> str | None:
    user_id = redis_client.get(f"refresh:{token_id}")
    return user_id


def rotate_refresh_token(old_token_id: str, user_id: str) -> str:
    # Delete old refresh token
    redis_client.delete(f"refresh:{old_token_id}")
    # Create new one
    return create_refresh_token(user_id)


def decode_access_token(token: str) -> dict:
    return jwt.decode(token, settings.secret_key, algorithms=[ALGORITHM])


def blacklist_token(jti: str, ttl_seconds: int) -> None:
    redis_client.setex(f"blacklisted:{jti}", ttl_seconds, "1")


def is_token_blacklisted(jti: str) -> bool:
    return bool(redis_client.exists(f"blacklisted:{jti}"))
```
</action>
<acceptance_criteria>
- `apps/api/app/auth/service.py` contains `def hash_password`
- `apps/api/app/auth/service.py` contains `def verify_password`
- `apps/api/app/auth/service.py` contains `def create_access_token`
- `apps/api/app/auth/service.py` contains `def create_refresh_token`
- `apps/api/app/auth/service.py` contains `def rotate_refresh_token`
- `apps/api/app/auth/service.py` contains `def blacklist_token`
- `apps/api/app/auth/service.py` contains `def is_token_blacklisted`
- `apps/api/app/auth/service.py` contains `redis_client = redis.Redis.from_url`
</acceptance_criteria>
</task>

<task id="01.4">
<title>Create auth dependencies (get_current_user, require_roles)</title>
<read_first>
- apps/api/app/auth/service.py (created in 01.3)
- apps/api/app/db/session.py (get_db dependency)
- apps/api/app/models/user.py (User model)
</read_first>
<action>
Create `apps/api/app/auth/dependencies.py`:

```python
from typing import Annotated

from fastapi import Cookie, Depends, HTTPException, Request, status
from jose import JWTError
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.auth.service import decode_access_token, is_token_blacklisted
from app.db.session import get_db
from app.models.user import User


async def get_current_user(
    request: Request,
    access_token: Annotated[str | None, Cookie()] = None,
    db: AsyncSession = Depends(get_db),
) -> User:
    credentials_exception = HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Could not validate credentials",
    )

    if not access_token:
        raise credentials_exception

    try:
        payload = decode_access_token(access_token)
    except JWTError:
        raise credentials_exception

    jti = payload.get("jti")
    if jti and is_token_blacklisted(jti):
        raise credentials_exception

    user_id = payload.get("sub")
    if not user_id:
        raise credentials_exception

    result = await db.execute(select(User).where(User.id == user_id))
    user = result.scalar_one_or_none()

    if not user or not user.is_active:
        raise credentials_exception

    return user


CurrentUser = Annotated[User, Depends(get_current_user)]


def require_roles(allowed_roles: list[str]):
    async def check_role(current_user: CurrentUser) -> User:
        if current_user.role.value not in allowed_roles:
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="Insufficient permissions",
            )
        return current_user
    return Depends(check_role)
```
</action>
<acceptance_criteria>
- `apps/api/app/auth/dependencies.py` contains `async def get_current_user`
- `apps/api/app/auth/dependencies.py` contains `CurrentUser = Annotated[User, Depends(get_current_user)]`
- `apps/api/app/auth/dependencies.py` contains `def require_roles`
- `apps/api/app/auth/dependencies.py` contains `Cookie()` (reads from httpOnly cookie)
- `apps/api/app/auth/dependencies.py` contains `is_token_blacklisted`
</acceptance_criteria>
</task>

## Verification
```bash
cd apps/api && uv run ruff check . && uv run ruff format --check .
cd apps/api && uv run python -c "from app.auth.service import hash_password, create_access_token; print('Auth service OK')"
cd apps/api && uv run python -c "from app.auth.dependencies import get_current_user, require_roles; print('Auth deps OK')"
```

## must_haves
- Password hashing with bcrypt
- JWT access token with jti, role, email, expiry
- Refresh token stored in Redis (UUID, not JWT)
- Token rotation on refresh (delete old, create new)
- Blacklist check on every authenticated request
- get_current_user reads from httpOnly cookie
- require_roles dependency for role checking
